# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# dotenv environment variable files — CMS_API_KEY 등 시크릿 포함, .env.example 제외 전부 커밋 금지
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 원본 업로드 압축본 — 같은 내용이 이미 디렉터리로 풀려 있어 중복
*.zip

# 검증 훅 산출물(스크린샷·측정 결과) — 실행할 때마다 새로 생성됨
**/.claude/hooks/out/
